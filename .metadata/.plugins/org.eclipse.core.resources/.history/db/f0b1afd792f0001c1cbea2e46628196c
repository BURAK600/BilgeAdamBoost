package bilgeadamweek3;

import java.util.Scanner;

import bilgeadamweek2.ToplamıBul;

public class MethodSum {
	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		
		System.out.println("Kac sayi gireceksiniz 2 ya da 3:");
		
		int sayi = scanner.nextInt();
		if(sayi ==2|| sayi ==3) {
			
			if (sayi ==2) {
				int n1 = scanner.nextInt();
				int n2 = scanner.nextInt();
				toplamıBul(n1,n2);
			}
			else {
				int n1 = scanner.nextInt();
				int n2 = scanner.nextInt();
				int n3 = scanner.nextInt();
				toplamıBul(n1, n2, n3);
				
			}
		}
	}

	private static void toplamıBul(int n1, int n2) {
		
		int toplam = n1+n2;
		System.out.println("iki sayinin toplami: " + toplam);
		
		
	}
	private static void toplamıBul(int n1, int n2,int n3) {
		
		int toplam = n1+n2+n3;
		System.out.println("iki sayinin toplami: " + toplam);

}
}
